# Copy to `.env` for local dev (same folder as package.json)
VITE_API_URL=http://localhost:8084

# Optional; must match backend PRIMARY_ADMIN_EMAIL so “who can delete admins” UI stays correct
# VITE_PRIMARY_ADMIN_EMAIL=sagormimmarriage@gmail.com
